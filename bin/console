#!/bin/sh

. bin/load_phantom
. bin/compile

env \
    MIX_ENV="${2:-dev}" \
    iex \
    --name "${1:-yst}-${MIX_ENV}" \
    --sname "${1:-yst}-${MIX_ENV}" \
    --cookie "yst-dev-test" \
    -S mix
