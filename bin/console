#!/bin/sh

export MIX_ENV="${1:-dev}"

. .env
. bin/load_phantom


mix compile && \
iex \
    --name "Yst-${MIX_ENV}-${UUID}" \
    --sname "Yst-${MIX_ENV}-${UUID}" \
    --cookie "Yst-${MIX_ENV}" \
    -S mix
