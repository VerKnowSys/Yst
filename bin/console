#!/bin/sh

. bin/load_phantom
. bin/compile

env \
    MIX_ENV="${1:-dev}" \
    YS_LOGIN="ys" \
    YS_PASS="***REMOVED***" \
    iex \
    --name "${2:-yst}-${MIX_ENV}" \
    --sname "${2:-yst}-${MIX_ENV}" \
    --cookie "yst-dev-test" \
    -S mix
