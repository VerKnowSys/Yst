#!/bin/sh

. bin/load_phantom
. bin/fetch_deps

# NOTE: skipping ost_test by default for now:
env \
    MIX_ENV="${1:-test}" \
    YS_LOGIN="ys" \
    YS_PASS="***REMOVED***" \
    mix test test/yst_test.exs
